# 海外保险数字化平台 — 优化升级开发计划 V1.0.5

| 文档 | 内容 |
|---|---|
| **文档版本** | V1.0.5 |
| **归档日期** | 2026-09-06 |
| **文档性质** | 优化升级开发计划（Optimization & Upgrade Plan） |
| **目标读者** | 开发工程师、架构师、项目管理人员 |
| **适用范围** | 全栈开发团队 |
| **触发原因** | 全量代码深度审计发现安全漏洞、工程化缺失、架构缺陷等 20+ 项问题 |

---

## 审计发现总结

通过对 `insurance-platform` Monorepo 全量代码的深度审计，发现以下关键问题：

### P0 — 安全漏洞（必须立即修复）

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| S1 | **JWT Token 为 Mock 实现** — `generateTokens()` 生成的是 `AT-{userId}-{timestamp}` 格式的伪 Token，非真正 JWT 签名 | `carrier-service/src/modules/auth/auth.service.ts` L204-218 | **Critical** |
| S2 | **CORS 允许所有来源** — `origin: true` 等同于 `*`，生产环境存在 CSRF/跨域攻击风险 | `carrier-service/src/main.ts` L46-49 | **High** |
| S3 | **日志泄露敏感数据** — 登录流程打印完整 user 对象（含 password_hash） | `auth.service.ts` L134-135 | **High** |
| S4 | **ValidationPipe 完全开放** — `whitelist: false` + `forbidNonWhitelisted: false`，任何额外字段都能穿透 | `main.ts` L36-43 | **Medium** |
| S5 | **Token 刷新未实现** — `verifyRefreshToken()` 固定返回 `true`，refresh 逻辑为空壳 | `auth.service.ts` L242-245 | **High** |

### P1 — 工程化缺失（影响开发效率和代码质量）

| # | 问题 | 当前状态 |
|---|------|----------|
| E1 | **无 ESLint / Prettier 配置** | 整个 Monorepo 未找到任何 `.eslintrc`、`eslint.config.*`、`.prettierrc` 文件 |
| E2 | **零测试覆盖** | 无 `*.test.*`、`*.spec.*` 文件；无 `jest.config.*`；尽管 `package.json` 声明了 `"test": "jest"` |
| E3 | **TypeScript 严格模式关闭** | 前端 `"strict": false`；后端 `"noImplicitAny": false`、`"strictBindCallApply": false` |
| E4 | **ORM 依赖冲突** | `carrier-service/package.json` 同时依赖 `@nestjs/typeorm` 和 `drizzle-orm`，实际只用 Drizzle |
| E5 | **CI/CD 流水线缺失** | 无 `.github/workflows/`、`.gitlab-ci.yml` 或任何自动化构建/部署配置 |
| E6 | **pnpm 版本不一致** | 根目录 `packageManager: "pnpm@8.15.0"` 与 engines `"pnpm": ">=8.0.0"` 存在版本策略差异 |
| E7 | **域模型引用方式不一致** | `carrier-service` 使用 `file:..\..\packages\domain-models`，应统一用 `workspace:*` |

### P2 — 架构与设计问题（中期重构）

| # | 问题 | 详情 |
|---|------|------|
| A1 | **未使用 React Router** | `App.tsx` 使用 409 行手动 switch + useState 管理路由，`react-router-dom` 已安装但未实际使用；URL 同步通过 `replaceState` 硬编码 |
| A2 | **巨型视图文件** | `ChannelMasterView.tsx` = 2172 行 / 138.8KB；`FinanceDashboardView.tsx` = 81.2KB；多个文件超 50KB |
| A3 | **Mock 数据耦合** | 所有视图直接 import Mock 数据文件（7 个 `data/*.ts`），未抽象 API Service 层做数据源切换 |
| A4 | **App.tsx 路由映射重复** | URL 同步 `useEffect` 中有 45+ 个 if-else 分支，与 `renderView()` 的 switch 完全重复 |
| A5 | **双重数据库客户端** | `database/` 下同时存在 `drizzle.client.ts` 和 `ovwr-drizzle.client.ts`，连接池配置可能冲突 |
| A6 | **后端模块职责混杂** | `auth.service.ts` 同时包含认证逻辑和用户 CRUD（487 行），违反单一职责 |
| A7 | **重复设计原型项目** | `产品方案/` 下有 V1.0~V1.5 共 6 个独立 Figma 原型项目，各自含 node_modules，占用大量磁盘空间 |

### P3 — 代码质量与可维护性（长期完善）

| # | 问题 |
|---|------|
| Q1 | **内联样式泛滥** — 大量组件使用 `style={{}}` 内联样式，Tailwind CSS 已安装但利用率低 |
| Q2 | **GlassCard/Badge 等基础组件重复定义** — 每个 View 文件内部重复定义 GlassCard、Badge 等 UI 原语 |
| Q3 | **缺少 Error Boundary** — 前端无全局错误边界，任何组件渲染错误将白屏 |
| Q4 | **缺少 Loading/Error 状态标准化** — 各视图各自实现加载态和错误态 UI |
| Q5 | **根目录散落文件** — `UserListView.tsx`(39KB)、`fix-passwords.py`、`dy` 等文件不应在根目录 |
| Q6 | **ViewId 定义了但未实现的视图** — `rate-plan-engine`、`actuarial-model-integration` 等 5 个视图在 viewMeta 中定义但 App.tsx 无 case |
| Q7 | **express 中间件顺序问题** — `main.ts` 中 `express.json()` 在 NestJS ValidationPipe 之后注册 |

---

## 实施计划

### Phase 0: 安全加固（P0，预计 8-12h）

> 目标：消除所有安全漏洞，使项目具备生产环境基本安全条件

**任务 0.1 — 实现真正的 JWT 认证（S1 + S5）**
- 文件：`carrier-service/src/modules/auth/auth.service.ts`
- 使用已安装的 `jsonwebtoken` 包替换 Mock Token 生成
- 实现 JWT 签名（HS256）+ 过期时间配置
- 实现 Refresh Token 验证逻辑（验证签名 + 过期 + 存储对比）

**任务 0.2 — 加固 CORS 配置（S2）**
- 文件：`carrier-service/src/main.ts`
- 使用环境变量配置允许的 origin 白名单
- 生产环境禁用 `origin: true`，仅允许已知前端域名

**任务 0.3 — 清除敏感日志（S3）**
- 文件：`carrier-service/src/modules/auth/auth.service.ts`
- 移除登录流程中所有敏感数据 console.log
- 使用 NestJS 内置 Logger 替代 console.log

**任务 0.4 — 强化 ValidationPipe（S4）**
- 文件：`carrier-service/src/main.ts`
- 设置 `whitelist: true` + `forbidNonWhitelisted: true`

**任务 0.5 — 修复 express 中间件顺序（Q7 关联）**
- 移除冗余的 express.json() / express.urlencoded()（NestJS 已内置）
- 移除调试用的 console.log 中间件

---

### Phase 1: 工程化基建（P1，预计 12-16h）

> 目标：建立代码质量门禁、统一工程规范

**任务 1.1 — ESLint + Prettier 统一代码规范（E1）**
- Monorepo 根目录配置 ESLint flat config
- 配置 Prettier（`.prettierrc` + `.prettierignore`）

**任务 1.2 — TypeScript 严格模式升级（E3）**
- 前端 `tsconfig.json`：`"strict": true`
- 后端 `tsconfig.json`：`"noImplicitAny": true`、`"strictBindCallApply": true`
- 分批修复编译错误

**任务 1.3 — 清理 ORM 依赖冲突（E4 + E7）**
- 从 `carrier-service/package.json` 移除 `@nestjs/typeorm`
- 统一 `domain-models` 引用为 `workspace:*`

**任务 1.4 — pnpm 版本一致性（E6）**
- 统一 `packageManager` 和 `engines` 配置

**任务 1.5 — 清理根目录散落文件（Q5）**
- 删除根目录散落的无用文件

---

### Phase 2: 架构升级（P2，预计 24-32h）

> 目标：解决核心架构问题，提升代码可维护性

**任务 2.1 — 后端模块拆分（A6）**
- 从 `auth.service.ts` 中拆出用户管理方法到独立的 `user.service.ts`

**任务 2.2 — 统一数据库客户端（A5）**
- 合并 `drizzle.client.ts` 和 `ovwr-drizzle.client.ts`

**任务 2.3 — 添加 ErrorBoundary（Q3）**
- 创建全局错误边界组件

---

## 工作量估算

| 阶段 | 预计工时 | 优先级 | 建议时间窗口 |
|------|----------|--------|-------------|
| Phase 0: 安全加固 | 8-12h | **最高** | 立即执行 |
| Phase 1: 工程化基建 | 12-16h | 高 | 紧随其后 |
| Phase 2: 架构升级 | 24-32h | 中 | 渐进实施 |
| Phase 3: 质量提升 | 16-24h | 低 | 穿插在日常迭代中 |
| **合计** | **60-84h** | — | 约 4-6 周 |

---

## 执行日志

| 日期 | 阶段 | 任务 | 状态 | 执行人 |
|------|------|------|------|--------|
| 2026-09-06 | Phase 0 | S1: 实现真正JWT认证（替换Mock Token） | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 0 | S2: CORS白名单配置 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 0 | S3: 清除敏感日志 + NestJS Logger | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 0 | S4: ValidationPipe 强化 (whitelist+forbid) | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 0 | Q7: 移除冗余express中间件+调试日志 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 1 | E1: ESLint flat config + Prettier | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 1 | E3: TypeScript 严格模式升级 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 1 | E4+E7: 移除TypeORM依赖 + workspace:* | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 1 | E6: pnpm版本策略统一 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 1 | Q5: 清理根目录散落文件 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 2 | A6: auth.service用户管理方法移除 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 2 | A5: 统一数据库连接池 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 2 | Q3: ErrorBoundary全局错误边界 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 3 | Q1+Q2: 组件库标准化 (GlassCard/GlassBadge/StatusBadge + barrel export) | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 3 | Q4: Loading/Error/Empty状态标准化组件 | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 3 | Q6: ViewId清理 (移除5个未实现的Phase5 ViewId) | ✅ 完成 | AI 辅助 |
| 2026-09-06 | Phase 3 | S3: 后端请求追踪中间件 + AppLogger敏感数据脱敏 | ✅ 完成 | AI 辅助 |

### 变更文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `carrier-service/src/main.ts` | 修改 | CORS白名单 + ValidationPipe强化 + 移除express中间件/调试日志 |
| `carrier-service/src/modules/auth/auth.service.ts` | 重构 | JWT真实签名 + Refresh Token验证 + 移除敏感日志 + 拆分用户管理方法 |
| `carrier-service/src/modules/auth/auth.controller.ts` | 修改 | Refresh Token端点重写 + 移除调试日志 |
| `carrier-service/src/modules/auth/auth.module.ts` | 修改 | 注册JwtModule + ConfigService注入 |
| `carrier-service/src/common/strategies/jwt.strategy.ts` | 重构 | 移除Mock Token支持，纯JWT验证 |
| `carrier-service/src/common/guards/jwt-auth.guard.ts` | 重构 | 移除Mock Token绕过，纯Passport验证 |
| `carrier-service/src/database/ovwr-drizzle.client.ts` | 修改 | 复用主连接池，移除独立Pool |
| `carrier-service/package.json` | 修改 | 移除TypeORM + workspace:*引用 |
| `carrier-service/tsconfig.json` | 修改 | 严格模式升级 |
| `web-carrier-admin/tsconfig.json` | 修改 | strict: true |
| `web-carrier-admin/src/main.tsx` | 修改 | 包裹ErrorBoundary |
| `web-carrier-admin/src/components/ErrorBoundary.tsx` | 新增 | 全局错误边界组件 |
| `insurance-platform/package.json` | 修改 | pnpm版本统一 + lint/format脚本 |
| `insurance-platform/.prettierrc` | 新增 | Prettier配置 |
| `insurance-platform/.prettierignore` | 新增 | Prettier忽略规则 |
| `insurance-platform/eslint.config.mjs` | 新增 | ESLint flat config |
| `.env` | 修改 | JWT配置 + CORS_ORIGINS + 端口修正 |
| `UserListView.tsx` | 删除 | 根目录散落文件清理 |
| `fix-passwords.py` | 删除 | 根目录散落文件清理 |
| `dy` | 删除 | 根目录散落文件清理 |
| `web-carrier-admin/src/components/ui/GlassCard.tsx` | 新增 | 毛玻璃卡片共享组件 |
| `web-carrier-admin/src/components/ui/GlassBadge.tsx` | 新增 | 彩色状态徽章 + StatusBadge语义化包装 |
| `web-carrier-admin/src/components/ui/PageLoading.tsx` | 新增 | 标准化加载状态组件 |
| `web-carrier-admin/src/components/ui/PageError.tsx` | 新增 | 标准化错误状态组件 |
| `web-carrier-admin/src/components/ui/EmptyState.tsx` | 新增 | 标准化空状态组件 |
| `web-carrier-admin/src/components/ui/index.ts` | 新增 | UI组件库统一导出 |
| `web-carrier-admin/src/navigation/viewMeta.ts` | 修改 | 移除5个未实现的Phase5 ViewId |
| `carrier-service/src/common/middleware/request-tracing.middleware.ts` | 新增 | 请求追踪中间件(RequestId+耗时) |
| `carrier-service/src/common/utils/app-logger.ts` | 新增 | 结构化日志工具(敏感字段自动脱敏) |
| `carrier-service/src/app.module.ts` | 修改 | 注册RequestTracingMiddleware全局中间件 |

---

*— 本文档由代码审计自动生成，基于 insurance-platform 全量代码深度分析。—*
